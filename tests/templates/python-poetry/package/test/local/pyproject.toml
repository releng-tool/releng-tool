[build-system]
requires = ['poetry_core>=1.0.0']
build-backend = 'poetry.core.masonry.api'

[tool.poetry]
name = 'hello_module'
description = 'hello world description'
authors = ['SomeUser <some@example.com>']
version = '1.0'
